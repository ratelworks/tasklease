# Changelog

## v0.1.0

- Added `compile`, `validate`, and `diff` commands.
- Added git-backed envelope compilation and deterministic resume checks.
- Added validation and diff tests with fixture coverage.

